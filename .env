# ----------------------------------------------------
# --- API Keys (Required) ---
# ----------------------------------------------------
# Replace these with your actual secret keys.
# Do not commit this file with your real keys to version control.

OPENAI_API_KEY=""
TAVILY_API_KEY=""


# ----------------------------------------------------
# --- Model & Agent Configuration (Optional) ---
# ----------------------------------------------------
# You can override these default settings if needed.

# The language model to use for generation and routing.
# Examples: "gpt-4", "gpt-3.5-turbo-instruct"
LLM_MODEL="gpt-3.5-turbo"

# The model to use for creating text embeddings for the vector store.
# "text-embedding-ada-002" is a standard and cost-effective choice.
EMBEDDING_MODEL="text-embedding-ada-002"

# The creativity of the LLM. Lower is more deterministic, higher is more creative.
# Value should be between 0.0 and 1.0.
TEMPERATURE=0.7

# The maximum number of tokens to generate in a single response.
MAX_TOKENS=1000


# ----------------------------------------------------
# --- Path Configuration (Optional) ---
# ----------------------------------------------------
# These paths define where persistent data is stored.
# The default values are recommended for the project structure.

# The directory where the persistent vector database will be stored.
VECTOR_DB_PATH="./data/vector_db"

# The directory where user-uploaded files will be temporarily saved.
UPLOAD_PATH="./data/uploads"